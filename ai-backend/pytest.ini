[pytest]
addopts = -q --cov=app --cov-report=term-missing
asyncio_mode = auto
testpaths = tests
pythonpath = .
markers =
    integration: tests that hit real external services (Neo4j Aura/local, MCP server, LLMs); skipped unless required env vars are set
